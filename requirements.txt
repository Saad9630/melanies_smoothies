#requirements.txt
snowflake-snowpark-python
streamlit
snowflake-snowpark-python==1.5.0  # Replace with your required version
streamlit==1.24.0  # Replace with your required version
pip install -r requirements.txt
git add requirements.txt
git commit -m "Fix requirements.txt for Streamlit app"
git push

